fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
websockets>=12.0
cryptography>=42.0
PyJWT>=2.8.0
httpx[http2]>=0.27.0
claude-agent-sdk>=0.1.0
numpy>=1.24.0
# Optional: google-genai>=1.0.0  # Only needed for Gemini embedding backend
# Jupyter deps are installed separately by install.sh (optional — enables execute_code tool)
# jupyter_client>=8.6.0
# ipykernel>=6.29.0

# External tools (not Python packages — installed separately by setup wizard):
# - Codex CLI: npm install -g @openai/codex
# - Ollama: https://ollama.com
